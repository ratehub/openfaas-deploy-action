# schedule.yml
name: 'Function Scheduled Re-deploy'
description: 'Re-deploy scheduled FaaS functions'
outputs:
  status:
    description: 'Whether the process succeeded or failed'
runs:
  using: 'docker'
  image: 'Dockerfile'
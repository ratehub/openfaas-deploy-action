const installFaasCli = require('../../common/installFaasCli');
const generateStackFile = require('./generateStackFile');
const generateResourceFile = require('./generateResourceFile');
const pushResourceFile = require('./pushResourceFile');


module.exports = {
    installFaasCli,
    generateStackFile,
    generateResourceFile,
    pushResourceFile,
};
